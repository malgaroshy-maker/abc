# Supabase Setup

1. Create a Supabase project.
2. Run `schema.sql` in SQL Editor.
3. Run `rls.sql` in SQL Editor.
4. Create storage bucket `logos` (private).
5. Enable email/password auth or SSO.
6. Add environment variables from `.env.example` to Vercel/hosting.
